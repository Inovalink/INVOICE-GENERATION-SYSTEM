import SignupWizard from '@/components/auth/SignupWizard';

/** Mobile entry: same flow as `/signup` (wizard starts on account type). */
export default function SignupAccountTypePage() {
  return <SignupWizard />;
}
